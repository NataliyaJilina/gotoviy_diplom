// Loader.tsx

import React from 'react';
import { LoaderContainer, HouseIcon } from './Loader.styled';

const Loader = () => {
  return (
    <LoaderContainer>
      <HouseIcon />
    </LoaderContainer>
  );
};

export default Loader;
